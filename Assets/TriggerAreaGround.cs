using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriggerAreaGround : MonoBehaviour
{
    /*
    private void OnTriggerEnter2D(Collider2D other)
    {
        if (other.tag == "Player")
        {
            //Destroy(gameObject);
            GameEvents.current.groundPound();
            Debug.Log("groundBreakable");
        }
    }*/
}
